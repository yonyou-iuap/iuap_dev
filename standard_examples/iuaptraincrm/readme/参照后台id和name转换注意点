1.在后台的service中进行id和name的转换时候需要将所对应实体中的名字写对，比如可能自动生成的是pk_org.name,但所对应的参照实体中的名字是psnname,那么就需要改成pk_org.psnname,
否则将会出现取不到值或者是路径与模型不符的错误。

2.在发生因为参照路径与模型不符、不能通过namespace和name找到实体等参照问题时候，请检查参照所建的元数据实体中的namespace、name与Java代码中的实体的@Entity的namespace、
name是否相同，然后发布元数据，进行测试，若还有问题请检查数据库中md_class表中是否新增了发布后的元数据信息，有时候会出现发布成功之后数据库表中信息没有更新，请多发布几次。